# 1. Install dependencies
npm install

# 2. Configure environment
cp .env.example .env
# (Add your Supabase & Cloudflare credentials)

# 3. Run locally
npm run dev

# 4. Deploy (after GitHub setup)
git push origin main  # Triggers automatic deployment